# Local SQLite database used by Prisma
DATABASE_URL=file:../db/custom.db

# Internal API key used by the local MCP services
# Replace this value with a strong random secret for local use.
MCP_SERVER_INTERNAL_API_KEY=replace-with-a-local-secret
