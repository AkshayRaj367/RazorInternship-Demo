import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Onyx — Guardrailed Agentic Commerce",
  description:
    "A revenue-growing shopping agent on Razorpay test mode: policy-stack guardrails (budget pacing, velocity, trust-adaptive limits, OTP), upsell engine, campaign orchestrator and a full money-action audit trail.",
  keywords: ["agentic commerce", "Razorpay", "guardrails", "OTP", "upsell", "campaigns", "audit trail"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
